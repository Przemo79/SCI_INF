
#include <iostream>
using namespace std;

int main() {
	
	char x;
	int il;
	
	cout << "Jaki znak mam wypisywac?: ";
	cin >> x;
	cout << "Ile razy mam go wypisac?: ";
	cin >> il;

	for (int a = 0; a < il; a++) {
		for (int b = 0; b < a; b++) {
			cout << x;
		}
		cout << x << "\n";
	}
}